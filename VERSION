#
# $Id: VERSION,v 4f408341f966 2015/01/01 18:55:53 nieves $
#

# The definitions here should appear without spaces around the '='
# sign, so that the file can be read by either make or sh.
#
# d = development
# p = prerelase (release candidate)
# r = release
name=nbspgislib
branch=0.5
releasenumber=1
releaselevel=p
update=4
version=${branch}.${releasenumber}${releaselevel}${update}
